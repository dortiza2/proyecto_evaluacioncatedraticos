## Variables públicas (no confidenciales)
# Base del backend (sin /v1). Si está vacío → modo offline
NEXT_PUBLIC_API_URL=
# Toggle para habilitar características AI en el front
NEXT_PUBLIC_AI_ENABLED=true